# Database Configuration
DB_HOST=db
DB_PORT=5432
DB_NAME=team_crm_dev
DB_USER=crm_user
DB_PASSWORD=your_secure_password_here

# JWT Security (Generate with: openssl rand -hex 32)
JWT_SECRET=your_secret_key_here_minimum_32_characters
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Office Location (GPS Validation)
# Update these coordinates to your office location
LAB_LATITUDE=28.544396761789827
LAB_LONGITUDE=77.19271651688473
LAB_RADIUS_METERS=200

# App Configuration
DEBUG=true
API_V1_PREFIX=/api/v1

# CORS Origins (comma-separated, update for production)
ALLOWED_ORIGINS=http://localhost:3001,http://localhost:3000

# Frontend Environment Variables
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
